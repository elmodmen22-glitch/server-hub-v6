{
  "name": "server-hub-v6",
  "private": true,
  "scripts": {
    "dev": "cross-env PORT=3001 concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "cross-env PORT=3001 npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix backend run build && npm --prefix frontend run build",
    "serve": "npm run build && npm --prefix backend start",
    "start": "npm --prefix backend start",
    "preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "cross-env": "^7.0.3"
  }
}
